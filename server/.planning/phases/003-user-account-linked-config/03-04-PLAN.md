---
phase: 003-user-account-linked-config
plan: 03-04
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - scripts/test-config-gate.js (new)
  - scripts/selftest.sh
status: executed
must_haves:
  truths:
    - "scripts/test-config-gate.js fails if any banned personal-config key appears inside chrome.storage.sync.{get,set} in sidepanel-prontuario.js or popup.src.js."
    - "scripts/test-config-gate.js fails if user-config-client.js is missing required exports, the self-write grace window, the signout cache-clear, or the legacy-cleanup logic."
    - "scripts/test-config-gate.js fails if either HTML surface lacks #auth-gate, the body[data-authed] CSS hook, or loads user-config-client AFTER popup.bundle.js."
    - "scripts/test-config-gate.js fails if the UserConfig model is missing any phase-003 column or if /api/me/config is not gated by @require_auth."
    - "scripts/selftest.sh runs the new tests as step [10/10] and exits non-zero on failure."
  artifacts:
    - "scripts/test-config-gate.js (20 tripwires)"
    - "scripts/selftest.sh updated"
---

<objective>
Lock in the invariants from plans 03-01..03-03 with regex-based tripwires
that fail loudly if any of them regress. Specifically: nothing in the
side-panel/popup code path may read or write personal-config keys via
chrome.storage.sync after this phase.

20 tripwires across 6 invariant groups:
  1. user-config-client presence + required exports + self-write window
     + signout cache-clear + legacy-cleanup
  2. HTML gate UI on both surfaces
  3. Script load order (user-config-client BEFORE popup.bundle.js)
  4. popup.src.js auth-gate observer + #auth-gate-signin-btn wiring
  5. chrome.storage.sync hygiene (banned-key scan via String.prototype.matchAll)
  6. Backend bookkeeping (UserConfig model fields + endpoint @require_auth)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/003-user-account-linked-config/ROADMAP.md
@scripts/test-debug-log.js
@scripts/selftest.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/test-config-gate.js with 20 tripwires</name>
  <files>scripts/test-config-gate.js</files>
  <action>
Mirror the regex-based tripwire pattern from scripts/test-debug-log.js.
Use Node's built-in test runner (node:test, node:assert/strict) — no deps.
Cover the 6 invariant groups listed above. The chrome.storage.sync hygiene
check uses matchAll over /chrome\.storage\.sync\.(get|set)\s*\(([\s\S]{0,400}?)\)/g,
then for each captured argument list, fails if any banned personal-config
key (BANNED_SYNC_KEYS) appears as a quoted string token.
  </action>
  <verify>
node --test scripts/test-config-gate.js
  </verify>
  <acceptance_criteria>
    - All 20 tests pass.
    - File is syntactically valid (node --test exits 0).
    - Use String.prototype.matchAll for regex iteration (not the regex .exec method, which trips a codebase security hook on writes).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire into scripts/selftest.sh as step [10/10]</name>
  <files>scripts/selftest.sh</files>
  <action>
Bump labels [N/9] → [N/10] and append a step 10 block that runs
node --test scripts/test-config-gate.js, redirects output to a temp log,
parses pass count from the TAP-ish summary, and sets FAIL=1 on non-zero exit.
  </action>
  <verify>
bash scripts/selftest.sh
  </verify>
  <acceptance_criteria>
    - All 10 steps pass green.
    - Step 10 reports the actual pass count (20 today).
  </acceptance_criteria>
</task>

</tasks>
