---
phase: 003-user-account-linked-config
plan: 03-03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - sidepanel/sidepanel-prontuario.js
  - popup/popup.src.js
  - background/service-worker.src.js
  - shared/* (new: shared/user-config-client.js)
autonomous: true
requirements: ["success-criterion-4", "success-criterion-5", "success-criterion-6"]
must_haves:
  truths:
    - "Every personal config read in the side panel + popup goes through chrome.storage.local[<key>_<uid>] cache, NOT chrome.storage.sync."
    - "Every personal config write goes: local cache → PATCH /api/me/config (debounced 400ms)."
    - "On sign-in: hydrator fetches GET /api/me/config and writes to local cache."
    - "On sign-out: chrome.storage.local.remove('config_<uid>') wipes the cache."
    - "Legacy chrome.storage.sync.prescriptionTemplates is deleted on first authed hydrate (no upload)."
  artifacts:
    - "shared/user-config-client.js: hydrator + cache + debounced patcher"
    - "background/service-worker.src.js: /api/me/config added to URL allowlist"
---

<objective>
Replace `chrome.storage.sync` reads/writes for all personal config keys
(prescriptionTemplates, voices, customInstructions, doctorName, autoCid,
autoClearSoap, autoTranscribeOnDictation) with a hydrator + local cache +
write-through PATCH pattern.

After this plan, the doctor's templates and voices follow them across
devices via the backend, not via Chrome's per-Google-account sync.
</objective>

<context>
@.planning/phases/003-user-account-linked-config/ROADMAP.md
@.planning/phases/003-user-account-linked-config/03-01-PLAN.md
@.planning/phases/003-user-account-linked-config/03-02-PLAN.md
</context>

<tasks>
(Task list will be authored when 03-03 starts.)
</tasks>
