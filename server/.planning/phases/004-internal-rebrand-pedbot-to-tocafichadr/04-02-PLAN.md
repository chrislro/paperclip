---
phase: 004-internal-rebrand-pedbot-to-tocafichadr
plan: 04-02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/keychain_helper.py
  - backend/emr_automation/database.py
  - backend/emr_automation/auth.py
  - backend/emr_automation/billing.py
  - backend/emr_automation/dashboard/app.py
  - backend/emr_automation/dashboard/routes.py
  - backend/emr_automation/dashboard/routes_clerk.py
status: pending
must_haves:
  truths:
    - "No `keychain_secret(\"pedbot-…\")` call site remains in backend/emr_automation/."
    - "backend/keychain_helper.py KEYCHAIN_SECRETS map uses tocafichadr-* names."
    - "Flask boots cleanly (curl /api/health → 200) after restart."
    - "Setup-instruction strings (e.g. auth.py:52) reference tocafichadr-* names."
---

<objective>
Single search-and-replace pass: `pedbot-` → `tocafichadr-` within Python
source files under `backend/emr_automation/` and `backend/keychain_helper.py`.

Affected sites (8 total):
- `backend/keychain_helper.py:45-51` — the KEYCHAIN_SECRETS map (7 entries)
- `backend/emr_automation/database.py:26` — `keychain_secret("pedbot-database-url")`
- `backend/emr_automation/auth.py:48` — `keychain_secret("pedbot-clerk-secret-key")`
- `backend/emr_automation/auth.py:52` — setup-instruction error message
- `backend/emr_automation/billing.py:26, 40, 68` — three Stripe key lookups
- `backend/emr_automation/dashboard/app.py:102` — `keychain_secret("pedbot-secret-key")`
- `backend/emr_automation/dashboard/routes_clerk.py:38` — `keychain_secret("pedbot-clerk-webhook-secret")`
- `backend/emr_automation/dashboard/routes.py:2214` — `has_secret("CLERK_SECRET_KEY", "pedbot-clerk-secret-key")` in the readiness endpoint
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Replace pedbot- → tocafichadr- in Python sources</name>
  <action>
For each file in `files_modified`, replace literal `"pedbot-` with `"tocafichadr-`
and `'pedbot-` with `'tocafichadr-`. Be careful NOT to touch logger names
(those have `pedbot.` not `pedbot-`) — those are handled in 04-03.
  </action>
  <verify>
grep -rIn '"pedbot-\|'\''pedbot-' backend/keychain_helper.py backend/emr_automation/ | wc -l
# Expected: 0
  </verify>
  <acceptance_criteria>
    - 0 remaining `pedbot-` references inside string literals in Python sources.
    - Logger names (`pedbot.extension_*`) untouched.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Restart Mini Flask + verify</name>
  <action>
After the commit lands and is rsync'd or git-pulled to the Mini:

```bash
ssh christianoliveira@100.97.14.32 \
  "launchctl kickstart -k gui/\$(id -u)/com.tocafichadr.cloud-api && \
   sleep 3 && curl -sf http://127.0.0.1:5050/api/health"
```
  </action>
  <verify>
Health returns `{"status":"ok",...}`. Tail `cloud-api-error.log` for any
`keychain_secret` lookup failure.
  </verify>
  <acceptance_criteria>
    - /api/health → 200
    - No "keychain entry not found" or SystemExit logs.
  </acceptance_criteria>
</task>

</tasks>
