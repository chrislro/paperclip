---
phase: 004-internal-rebrand-pedbot-to-tocafichadr
plan: 04-03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - backend/emr_automation/dashboard/routes.py
  - package.json
status: pending
must_haves:
  truths:
    - "getLogger('pedbot.*') calls renamed to getLogger('tocafichadr.*')."
    - "package.json `name` field is `tocafichadr-extension`."
    - "package.json GitHub URLs use `tocafichadr-extension`."
---

<objective>
Cosmetic-but-visible renames that don't affect operational secrets:

1. `routes.py:1679` — `logging.getLogger("pedbot.extension_error")` → `logging.getLogger("tocafichadr.extension_error")`
2. `routes.py:1704` — `logging.getLogger("pedbot.extension_debug")` → `logging.getLogger("tocafichadr.extension_debug")`
3. `package.json:2` — `"name": "pedbot-extension"` → `"name": "tocafichadr-extension"`
4. `package.json:20` — `"url": "git+https://github.com/chrislro/pedbot-extension.git"` → `git+https://github.com/chrislro/tocafichadr-extension.git`
5. `package.json:27` — `"url": "https://github.com/chrislro/pedbot-extension/issues"` → `.../tocafichadr-extension/issues`
6. `package.json:29` — `"homepage": "https://github.com/chrislro/pedbot-extension#readme"` → `.../tocafichadr-extension#readme`

After updating `package.json`, run `npm install` to regenerate `package-lock.json` so the lockfile's `name` field matches.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: routes.py logger names</name>
  <files>backend/emr_automation/dashboard/routes.py</files>
  <verify>
grep -n 'getLogger("pedbot' backend/emr_automation/dashboard/routes.py
# Expected: 0 matches
grep -n 'getLogger("tocafichadr' backend/emr_automation/dashboard/routes.py
# Expected: 2 matches (extension_error, extension_debug)
  </verify>
</task>

<task type="auto">
  <name>Task 2: package.json + lockfile</name>
  <files>package.json, package-lock.json</files>
  <action>
Edit package.json (4 string replacements), then run `npm install --package-lock-only` to regenerate the lockfile name field without re-fetching dependencies.
  </action>
  <verify>
grep -n '"name": "tocafichadr-extension"' package.json package-lock.json
# Both files should match.
  </verify>
</task>

</tasks>
